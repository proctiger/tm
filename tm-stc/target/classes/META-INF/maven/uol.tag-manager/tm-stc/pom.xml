<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>uol.tag-manager</groupId>
		<artifactId>tag-manager</artifactId>
		<version>1.4.8-SNAPSHOT</version>
	</parent>
	<artifactId>tm-stc</artifactId>
	<name>tm-stc</name>
	<url>http://maven.apache.org</url>
	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
	</properties>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-assembly-plugin</artifactId>
				<configuration>
					<descriptors>
						<descriptor>src/main/assembly/assembly.xml</descriptor>
					</descriptors>
				</configuration>
				<executions>
					<execution>
						<id>${project.artifactId}</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>2.3.2</version>
				<configuration>
					<encoding>${project.build.sourceEncoding}</encoding>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<version>2.4.3</version>
				<configuration>
					<encoding>${project.build.sourceEncoding}</encoding>
				</configuration>
			</plugin>
		</plugins>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
				<includes>
					<include>**/*</include>
				</includes>
			</resource>
		</resources>
	</build>
	<!--
	<profiles>
		<profile>
			<id>deploy</id>
			<build>
				<plugins>
					<plugin>
						<groupId>com.goldin.plugins</groupId>
						<artifactId>maven-copy-plugin</artifactId>
						<executions>
							<execution>
								<id>copy-bin</id>
								<phase>install</phase>
								<goals>
									<goal>copy</goal>
								</goals>
								<configuration>
									<resources>
										<resource>
											<targetPath>scp://${deploy.user}:${deploy.pass}@${deploy.instance}:/tmp/${deploy.user}.${deploy.build}</targetPath>
											<file>${project.build.directory}/${deploy.build}</file>
										</resource>
									</resources>
								</configuration>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<groupId>com.goldin.plugins</groupId>
						<artifactId>maven-sshexec-plugin</artifactId>
						<executions>
							<execution>
								<id>install-bin</id>
								<phase>install</phase>
								<goals>
									<goal>sshexec</goal>
								</goals>
								<configuration>
									<echoPwd>false</echoPwd>
									<echoCommands>false</echoCommands>
									<location>scp://${deploy.user}:${deploy.pass}@${deploy.instance}:/opt/UOLI/webapps/tm-stc</location>
									<commands>
										<command>sudo rm -rf *</command>
										<command>sudo cp /tmp/${deploy.user}.${deploy.build} .</command>
										<command>sudo unzip ${deploy.user}.${deploy.build}</command>
										<command>sudo rm ${deploy.user}.${deploy.build}</command>
									</commands>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
			<properties>
				<deploy.instance>a1-tagmanager-s-prt1.host.intranet</deploy.instance>
				<deploy.build>${project.artifactId}-${project.version}-bin.zip</deploy.build>
			</properties>
		</profile>
	</profiles>
	-->
</project>
